# openpyxl, pandas, and pytest versions are governed by
# backend/requirements.txt and backend/requirements-dev.txt (ticket #114)
# once this module is installed as part of the backend — kept in sync here
# for anyone running the scripts standalone.
openpyxl>=3.1,<3.2
pandas>=3.0,<3.1
pytest>=8.3,<9
