sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.2,<3.3
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
boto3>=1.34,<2
# app/services/cutting_jobs.py (reused as-is, see Dockerfile) pulls in
# app/services/timestamp_excel.py, which needs this directly — not just
# transitively through the real `assist` package's own install (Dockerfile
# only, never this requirements.txt — see worker/requirements.txt's same
# "no GPU/heavy deps here" convention).
openpyxl>=3.1,<3.2
